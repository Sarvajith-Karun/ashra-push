/** @type {import('next').NextConfig} */
const nextConfig = {
  eslint: {
    // Disable specific ESLint rules or override configurations
    ignoreDuringBuilds: true,
  },
};

export default nextConfig;
